import { Link } from "react-router-dom";

function Home() {
  return (
    <div style={styles.container}>
      <h1 style={styles.title}>MyLangApp</h1>
      <p style={styles.subtitle}>Practice English with flashcards & quizzes 🚀</p>

      <div style={styles.buttons}>
        <Link to="/practice" style={styles.button}>Start Practicing</Link>
        <Link to="/login" style={styles.button}>Login</Link>
      </div>
    </div>
  );
}

const styles = {
  container: {
    minHeight: "100vh",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    background: "linear-gradient(135deg, #1f1c2c, #928dab)",
    color: "#fff",
    fontFamily: "Arial, sans-serif",
    textAlign: "center"
  },
  title: {
    fontSize: "3rem",
    marginBottom: "1rem",
    color: "#ff4d4d"
  },
  subtitle: {
    fontSize: "1.2rem",
    marginBottom: "2rem"
  },
  buttons: {
    display: "flex",
    gap: "1rem"
  },
  button: {
    padding: "0.8rem 1.5rem",
    background: "#4CAF50",
    color: "#fff",
    borderRadius: "8px",
    textDecoration: "none",
    fontWeight: "bold",
    transition: "0.3s"
  }
};

export default Home;

